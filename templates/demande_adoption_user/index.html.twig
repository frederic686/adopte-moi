{% extends 'base.html.twig' %}

{% block title %}Demande d’adoption – {{ animal.nom }}{% endblock %}

{# Si ton base.html.twig expose un bloc "content", utilise "content". #}
{% block content %}
  <div class="example-wrapper" style="max-width:800px;margin:1rem auto;">
    <h1>Demande d’adoption pour « {{ animal.nom }} »</h1>

    <div style="display:flex; gap:20px; align-items:flex-start; margin:20px 0;">
      <img src="{{ animal.photo ?: '/uploads/animals/default.png' }}"
           alt="{{ animal.nom }}"
           style="width:200px;height:200px;object-fit:cover;border-radius:8px;border:1px solid #ddd">
      <div>
        <p><strong>Race :</strong> {{ animal.race }}</p>
        <p><strong>Sexe :</strong> {{ animal.sexe }}</p>
        <p><strong>Âge :</strong> {{ animal.age }} ans</p>
      </div>
    </div>

    {{ form_start(form) }}
      {{ form_row(form.message) }}
      <button type="submit" class="btn">📩 Envoyer ma demande</button>
    {{ form_end(form) }}

    <p style="margin-top:20px;">
      <a href="{{ path('animal_public_show', { id: animal.id }) }}">⬅ Retour à la fiche</a>
    </p>
  </div>
{% endblock %}
